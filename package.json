{
  "name": "scu-lens",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.19 <23" },
  "scripts": {
    "test": "node --test",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:media": "node scripts/validate-media.mjs",
    "check:links": "node scripts/check-built-links.mjs",
    "check:deploy-config": "node scripts/validate-deployment-config.mjs",
    "prepare:pages": "node scripts/prepare-pages-artifact.mjs",
    "check:live": "node scripts/check-live-deployments.mjs",
    "dev:site": "astro dev --host 127.0.0.1 --port 4321",
    "build:site": "astro build",
    "check:site": "astro check",
    "preview:site": "astro preview --host 127.0.0.1 --port 4322",
    "dev": "astro dev --host 127.0.0.1 --port 4321",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 4322",
    "check": "astro check",
    "import:photos": "powershell -ExecutionPolicy Bypass -File scripts/import-photo-library.ps1"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^7.1.6"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.3"
  }
}
